<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.demo.sky.mapper.OrderMapper">


    <select id="getByNumberAndUserId" resultType="com.demo.sky.dao.Orders">
        select *
        from orders
        where number = #{orderNumber}
          and user_id = #{userId};
    </select>


    <select id="pageOrder" resultType="com.demo.sky.dao.Orders">
        select *
        from orders
        <where>
            <if test="number!=null and number!=''">and number like concat('%',#{number},'%')</if>
            <if test="phone!=null and phone!=''">and phone like concat('%',#{phone},'%')</if>
            <if test="userId!=null and userId!=''">and user_id=#{userId}</if>
            <if test="status!=null and status!=''">and status=#{status}</if>
            <if test="beginTime!=null and beginTime!=''">and order_time&gt;=#{beginTime}</if>
            <if test="endTime!=null and endTime!=''">and order_time&lt;=#{endTime}</if>
        </where>
        order by order_time desc
    </select>


    <select id="countStatus" resultType="java.lang.Integer">
        select count(*)
        from orders where status=#{status};
    </select>


    <select id="getByStatusAndOrderTime" resultType="com.demo.sky.dao.Orders">
        select *
        from orders where status=#{status} and order_time&lt;#{orderTime};
    </select>


    <select id="sumByMap" resultType="java.lang.Double">
        select sum(amount) from orders
        <where>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="begin != null">
                and order_time &gt;= #{begin}
            </if>
            <if test="end != null">
                and order_time &lt;= #{end}
            </if>
        </where>
    </select>


    <select id="countByMap" resultType="java.lang.Integer">
        select count(id) from orders
        <where>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="begin != null">
                and order_time &gt;= #{begin}
            </if>
            <if test="end != null">
                and order_time &lt;= #{end}
            </if>
        </where>
    </select>


    <select id="getSalesTop10" resultType="com.demo.sky.dto.GoodsSalesDTO">
        select od.name name,sum(od.number) number from order_detail od ,orders o
        where od.order_id = o.id
        and o.status = 5
        <if test="begin != null">
            and order_time &gt;= #{begin}
        </if>
        <if test="end != null">
            and order_time &lt;= #{end}
        </if>
        group by name
        order by number desc
        limit 0, 10
    </select>
</mapper>